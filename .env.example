# ============================================================================
# Gmail Scanner - Configuração IMAP
# ============================================================================
#
# Esta aplicação usa IMAP para acessar o Gmail (sem necessidade de OAuth)
#
# IMPORTANTE: Copie este arquivo para .env e configure conforme necessário
#   cp .env.example .env
#
# ============================================================================

# =============================================================================
# Configuração da Aplicação
# =============================================================================
# APP_ENV: development, staging, production
APP_ENV=development

# LOG_LEVEL: debug, info, warn, error
LOG_LEVEL=info

# JWT_SECRET: Chave secreta para assinatura de tokens JWT
# Em produção, use uma chave forte e aleatória
JWT_SECRET=your-secret-key-change-in-production-use-random-string

# =============================================================================
# Configuração IMAP (Gerenciado pelo usuário no login)
# =============================================================================
# O usuário fornece email e senha de app do Gmail no formulário de login
# Não é necessário configurar credenciais aqui

# Servidor IMAP do Gmail (não precisa alterar)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# =============================================================================
# Volumes (Paths para dados)
# =============================================================================
# VOLUMES_PATH: Caminho local onde os dados serão armazenados
# Deixe vazio para usar ./data no diretório do projeto
VOLUMES_PATH=

# =============================================================================
# Desenvolvimento
# =============================================================================
# DOCKER_BUILDKIT: Usar BuildKit para builds mais rápidos
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1
